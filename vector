#include<iostream>
#include<vector>
using namespace std;
int main()
{
	
		vector<int>v1;
		for(int i=0; i<10; i++)
		{
			v1.push_back(10*(i+1));
		}
		 v1.push_back(12);
		 v1.pop_back();
		 v1.pop_back();
	//	 cout<<v1.front();
	//	 cout<<v1.back();
	    vector<int>:: iterator it=v1.begin();
	    v1.insert(it+3,374);
	   // for(int i=0; i<v1.size(); i++)
	    cout<<v1.at(i)<<endl;
		
}
